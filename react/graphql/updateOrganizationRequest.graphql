mutation UpdateOrganizationRequest($id: ID!, $status: String!, $notes: String) {
  updateOrganizationRequest(id: $id, status: $status, notes: $notes)
    @context(provider: "demob2bstore.b2b-organizations-graphql") {
    id
    status
    message
  }
}
