mutation RemoveUser($id: ID!, $userId: ID, $email: String!, $clId: ID!) {
  removeUser(id: $id, userId: $userId, email: $email, clId: $clId)
    @context(provider: "demob2bstore.b2b-organizations-graphql") {
    status
  }
}
