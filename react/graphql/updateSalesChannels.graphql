mutation UpdateSalesChannels($channels: [SalesChannelsInput]!) {
  saveSalesChannels(channels: $channels)
    @context(provider: "demob2bstore.b2b-organizations-graphql") {
    status
    message
  }
}
