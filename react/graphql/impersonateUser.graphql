mutation ImpersonateUser($clId: ID, $userId: ID) {
  impersonateUser(clId: $clId, userId: $userId)
    @context(provider: "demob2bstore.b2b-organizations-graphql") {
    id
    status
    message
  }
}
